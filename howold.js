console.log("linked");

// 1. How many hours are in a year
var daysInYear = 365;
var hoursInDay = 24;
var hoursInYear = daysInYear * hoursInDay;
console.log(hoursInYear);

// 2. How many minutes are in a decade
var yearsInDecade = 10;
var minutesInHour = 60;
console.log("minutes in decade", yearsInDecade * daysInYear * hoursInDay * minutesInHour);

// 3. How many seconds old they are
var secondsInMinute = 60;
var ageOfBrooke = 24;
console.log ("How old am I in seconds:", ageOfBrooke * daysInYear * hoursInDay * minutesInHour * secondsInMinute)

// 4. If they are older than some arbitrary number, console "I'm old", else "I'm young"
if (ageInSeconds >)